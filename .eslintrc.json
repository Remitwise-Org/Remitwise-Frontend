{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "CallExpression[callee.name='useEffect'][arguments.length=1]",
        "message": "useEffect with no dependency array is banned. Should be either explicit deps or an // biome-ignore with reason."
      },
      {
        "selector": "CallExpression[callee.name='useState'][arguments.0.type='CallExpression']",
        "message": "useState with an initial function call runs on every render. Use a lazy initializer: useState(() => myExpensiveInit())"
      }
    ]
  }
}
